var cookieService = Object.assign(
    {},
    (function () {
        return {
            set: function (c, d, a) {
                var b = new Date();
                b.setTime(b.getTime() + 864e5 * a);
                a = "expires\x3d" + b.toUTCString();
                document.cookie = c + "\x3d" + d + ";" + a + ";path\x3d/";
            },
            get: function (c) {
                c += "\x3d";
                for (var d = decodeURIComponent(document.cookie).split(";"), a = 0; a < d.length; a++) {
                    for (var b = d[a]; " " == b.charAt(0); ) b = b.substring(1);
                    if (0 == b.indexOf(c)) return b.substring(c.length, b.length);
                }
                return "";
            },
            remove: function (c, d) {
                var a = { expires: -1 };
                void 0 !== d && (a = Object.assign(d, a));
                this.set(c, "", a);
            },
            setExpiresWhenClosingBrowserWindow: function (c, d) {
                document.cookie = c + "\x3d" + d + ";expires\x3d0;path\x3d/";
            },
        };
    })()
);
